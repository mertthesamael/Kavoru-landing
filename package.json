{
  "name": "kavoru-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Landing page for Kavoru — the Elysia starter for humans",
  "type": "module",
  "scripts": {
    "og:generate": "bun run scripts/generate-og.ts",
    "dev": "vite",
    "build": "bun run og:generate && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "vite": "^6.3.5"
  }
}
